(set n 10000000)

(set t (table [Symbol Time Price] 
    (list (take n [AAPL IBM MSFT GOOG]) 
          (as 'Time (til n))
          (as 'F64 (take n [123 11 456 33])))))


;; (select {from: t p: (max Price) by: (xwin Time 1000)})

